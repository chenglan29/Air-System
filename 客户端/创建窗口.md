# 创建窗口
您可以使用
```js
new Window(value = {
    width:w/10*8,
    height:h/10*8,
    top:Math.floor(Math.random()*(h/10*2-45)),
    left:Math.floor(Math.random()*w/10*2),
    title:"无标题",
    content:"",
    src:""
});
```
注:src作为一个iframe窗口的URL，src不为空时将忽略content