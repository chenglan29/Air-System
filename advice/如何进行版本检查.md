如何进行版本检查？

在 initServer/ 下创建一个新文件，
命名格式为 \[应用名\].versionCheck.js
```js
module.exports = (versionNum,versionName,logger) => {
    if(versionNum != 5)logger.warn(`软件 [应用名] 的版本([应用版本])与当前服务器版本(${versionName})不匹配，也许无法运行`);
}
```